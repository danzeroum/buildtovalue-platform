# Gate de piloto (D17, plano §8.4) — checklist vivo de evidências

> BLOQUEANTE: nenhum piloto com cliente real antes de TODOS os itens
> evidenciados. Cada linha aponta a evidência quando existir; itens sem
> evidência ficam explícitos como PENDENTES.
>
> **Auditoria a/b/c dos 13 itens + necessidades de infra** (secret manager, KMS,
> WAL imutável — o que `KeyProvider`/`AIProvider` esperam):
> `docs/privacy/gate-piloto-auditoria.md`.

| # | Critério (plano §8.4) | Evidência / Estado |
|---|---|---|
| 1 | RLS com `SET LOCAL` testada; API sem BYPASSRLS; papel de migração separado | `packages/db/tests/rls-isolation.test.ts` (suíte permanente, 11 tabelas com FORCE RLS) — VERDE desde F1, ampliada na F2. |
| 2 | Redaction testada; TLS; secrets em secret manager; audit de dependências verde | Redaction leak-fail verde (`packages/observability/tests/redaction.test.ts`). `pnpm audit` no CI ATIVO (#45) — **verde COM 1 exceção documentada** (`GHSA-qwww-vcr4-c8h2`, inaplicável — ver `docs/security/audit-exceptions.md`). TLS/secret manager: PENDENTES (infra de piloto). **Imagens do deploy com gate de máquina:** job `imagens` constrói os 4 `deploy/Dockerfile.*` a cada PR (matrix, `fail-fast: false`, sem registry). Proteção PROVADA por quebra proposital (flag inválido no `pnpm install` do Dockerfile.api — a mesma classe do `--legacy` que foi à produção), em branch descartável: [rodada 30411022175](https://github.com/danzeroum/buildtovalue-platform/actions/runs/30411022175) → `imagem (api)` **failure** no passo "Build de deploy/Dockerfile.api"; `imagem (migrate)`, `imagem (worker)` e `imagem (console)` **success**, concluídas DEPOIS da falha (00:24:49 / 00:24:53 / 00:25:01 vs falha às 00:24:18) — `fail-fast: false` comprovado no ato. Linha de base verde: [rodada 30409508266](https://github.com/danzeroum/buildtovalue-platform/actions/runs/30409508266), 6/6. |
| 3 | Backup automatizado + restore ensaiado e documentado | Ensaio de 2026-07-22 em `docs/runbooks/database.md`. Automação: script + agendamento **existem** (`infra/docker/backup.sh` com modo Docker, piso de tamanho e log de exit code; `infra/docker/backup.cron` diário) e foram testados contra Postgres 16 real nos dois caminhos (dump válido de 219 objetos com sha256 conferido; banco fora do ar → `exit 1` logado e nenhum arquivo). Cópia externa por `rclone` (`BACKUP_REMOTE`) com sha256 do objeto remoto conferido, testada nos três caminhos: destino ausente → modo degradado com aviso; destino ok → hash idêntico; destino inacessível → `exit 1` com o dump local preservado. **PENDENTE: (a) instalar o crontab na VPS, (b) definir o destino remoto — depende da decisão de infra do piloto —, (c) registrar aqui evidência de execuções reais e (d) o ensaio de restauração A PARTIR da cópia remota.** O gate pede o backup ACONTECENDO fora da VPS, não a capacidade de fazê-lo. |
| 4 | Ledger sem dados pessoais (teste); criptografia ativa para `sensitive` com KeyProvider em secret manager/KMS — **chave estática REPROVA o gate** (D20) | Teste nomeado "ledger nunca contém conteúdo pessoal" VERDE (`packages/db/tests/lgpd-seam.test.ts`); cifra ativa (F2.6). KeyProvider de KMS: PENDENTE (hoje: provedor de ambiente dev/CI — reprova de propósito). |
| 5 | Plano de incidentes escrito + simulação executada e documentada (G-LGPD-4) | **Plano escrito:** `docs/runbooks/incidentes.md` — severidades S1–S4 com critério, papéis (e a ordem quando uma pessoa os acumula), comunicação incl. LGPD art. 48, contenção por tipo, restauração **a partir da cópia remota** em banco descartável, critério de encerramento com janela de perda quantificada, e post-mortem sem culpado que obriga alterar o runbook. **PENDENTE: a SIMULAÇÃO.** O gate pede plano **e** ensaio executado — documento sem ensaio reprova. O ensaio depende do crontab instalado e do `BACKUP_REMOTE` definido (decisão de infra do piloto); ensaiar contra o dump local provaria o caminho já ensaiado em 22/07, não o que o gate pede. |
| 6 | **Retorno do repo `buildtovalue-platform` a PRIVATE** — D1 revisado em 22/07: público de propósito até o fechamento da v1 (acesso direto de arquiteto/analistas); o retorno a Private é ITEM DO GATE. | PENDENTE (decisão do dono, 22/07 — registrada em `docs/pendencias.md`). Verificação oficial: método externo (selo do cabeçalho/404 anônimo), executada pelo desenvolvedor e registrada aqui. |
